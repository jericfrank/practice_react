// import { fromJS } from 'immutable';
// import { makeSelectTrashDomain } from '../selectors';

// const selector = makeSelectTrashDomain();

describe('makeSelectTrashDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
