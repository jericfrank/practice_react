// import { fromJS } from 'immutable';
// import { makeSelectSentDomain } from '../selectors';

// const selector = makeSelectSentDomain();

describe('makeSelectSentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
