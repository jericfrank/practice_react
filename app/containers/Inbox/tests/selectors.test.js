// import { fromJS } from 'immutable';
// import { makeSelectInboxDomain } from '../selectors';

// const selector = makeSelectInboxDomain();

describe('makeSelectInboxDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
